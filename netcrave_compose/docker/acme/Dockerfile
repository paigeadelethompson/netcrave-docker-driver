FROM netcrave:docker

ENV ACME_KEY_SIZE 

ENV ACME_USER_AGENT

ENV ACME_STAGING_URL

ENV ACME_PROD_URL

ENV ACME_EMAIL

ENV ACME_TOS_AGREE

ENV ACME_PROCESSOR_QUEUE_ADDR

ENV NETCRAVE_PRESHARED_KEY

ENV NETCRAVE_NONCE

ENV ACME_LISTEN_ADDR

CMD acmed
